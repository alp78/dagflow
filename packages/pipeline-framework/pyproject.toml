[project]
name = "dagflow-pipeline-framework"
version = "0.1.0"
description = "Shared Dagflow pipeline registration and runtime contracts"
requires-python = ">=3.13"
dependencies = [
  "pydantic>=2.11.0",
  "psycopg[binary]>=3.2.9",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
