# Generated by ops/scripts/sync_config.py — do not edit manually.
# Edit config.json at the repo root, then run: make sync-config

name: dagflow
version: 1.0.0
config-version: 2

profile: dagflow

model-paths:
  - "../workflows/security_shareholder/dbt/models"
snapshot-paths:
  - "snapshots"
test-paths:
  - "../workflows/security_shareholder/dbt/tests"
macro-paths:
  - "macros"

target-path: target
clean-targets:
  - target
  - dbt_packages
  - logs

models:
  dagflow:
    +materialized: table

snapshots:
  dagflow:
    +target_schema: marts

on-run-end:
  - "{{ log_dq_results(results) }}"
